services:
  app:
    build: 
      context: ./docker-ffmpeg
      dockerfile: Dockerfile
    image: node-ffmpeg:latest
    ports:
     - "3003:3000"
    deploy:
      resources:
        limits:
          cpus: '2' 
          memory: 20g 